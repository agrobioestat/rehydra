% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_reduction_functions.R
\name{recovery_period_metrics}
\alias{recovery_period_metrics}
\title{Recovery-Period and Total-Reduction Indices}
\usage{
recovery_period_metrics(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  recovery_level = 0.95,
  reduction_level = 0.95,
  baseline_summary = c("mean", "median", "last")
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for the calculations.}

\item{group_by}{Grouping columns.}

\item{recovery_level}{Fraction of the baseline that defines "recovered".
The default \code{0.95} means the response has to climb back to within 5\% of the
pre-drought level.}

\item{reduction_level}{Fraction of the baseline below which the response is
considered impaired, used for \code{reduction_period}.}

\item{baseline_summary}{How the pre-drought baseline is summarized:
\code{"mean"}, \code{"median"} or \code{"last"}.}
}
\value{
A tibble with one row per group and cycle and columns \code{baseline},
\code{drought_minimum}, \code{stress_detected}, \code{time_to_minimum}, \code{recovery_period},
\code{recovery_censored}, \code{time_recovered}, \code{reduction_period},
\code{total_reduction}, \code{total_reduction_relative} and \code{recovery_level}.
}
\description{
Compute how long a plant takes to return to its pre-drought level and how
much performance it loses in total over the episode.
}
\details{
Write \code{B} for the baseline (the pre-drought level), \code{y(t)} for the observed
trajectory, \code{t_str} for the onset of the stress, \code{t_min} for the time of the
trough and \code{t_end} for the last observation of the cycle. The deficit
function is

\deqn{d(t) = \max(0,\; B - y(t)).}

\strong{Recovery period.} The time the plant needs to climb back to the
pre-drought level, measured from the trough:

\deqn{RP = t_{rec} - t_{min}, \qquad
  t_{rec} = \min\{t \ge t_{min} : y(t) \ge \alpha B\}}

with \eqn{\alpha =} \code{recovery_level}. The crossing time \eqn{t_{rec}} is
obtained by linear interpolation between the two observations that bracket
it, so \code{recovery_period} is not rounded to the sampling grid. When the series
ends before the threshold is reached the observation is \strong{right-censored}:
\code{recovery_period} is \code{NA} and \code{recovery_censored} is \code{TRUE}. Treating those
as \code{NA} rather than as "the length of the experiment" avoids a downward bias
that would make the slowest genotypes look like the fastest.

\strong{Total reduction.} The whole performance loss caused by the episode, the
area between the baseline and the trajectory:

\deqn{TR = \int_{t_{str}}^{t_{end}} d(t)\, dt}

evaluated by the trapezoidal rule. It is expressed in
\eqn{[\mathrm{value}] \times [\mathrm{time}]} units, so it is comparable only
within a variable; \code{total_reduction_relative} divides it by the undisturbed
integral \eqn{B \times (t_{end} - t_{str})} and is a dimensionless fraction of
the potential performance that was lost, comparable across variables and
experiments.

Floring the deficit at zero matters: without it, overcompensation after
rewatering (\code{y > B}) would subtract from the accumulated damage and a plant
that suffered badly and then overshot could report a total reduction close to
zero.

Both quantities are read off a continuously sampled physiological trajectory:
the integral replaces the sum over discrete observation periods used when the
same components are computed on annual growth series, and the crossing that
closes the recovery period is interpolated rather than rounded to the
sampling grid.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
recovery_period_metrics(segments)
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x}.
}
\seealso{
\code{\link[=mean_reduction_metrics]{mean_reduction_metrics()}}, \code{\link[=curve_shape_metrics]{curve_shape_metrics()}}, \code{\link[=resilience_index]{resilience_index()}}.
}
