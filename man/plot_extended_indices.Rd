% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_curve_functions.R
\name{plot_extended_indices}
\alias{plot_extended_indices}
\title{Plot the Period and Reduction Components}
\usage{
plot_extended_indices(
  period_results,
  reduction_results = NULL,
  facet_by = "variable",
  drop_undetected = TRUE
)
}
\arguments{
\item{period_results}{Output from \code{\link[=recovery_period_metrics]{recovery_period_metrics()}}.}

\item{reduction_results}{Output from \code{\link[=mean_reduction_metrics]{mean_reduction_metrics()}}.}

\item{facet_by}{Faceting columns.}

\item{drop_undetected}{Logical; drop rows where no stress was detected.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot the Period and Reduction Components
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = water_potential
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
plot_extended_indices(recovery_period_metrics(segments), mean_reduction_metrics(segments))
}
