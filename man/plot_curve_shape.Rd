% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_curve_functions.R
\name{plot_curve_shape}
\alias{plot_curve_shape}
\title{Plot Curve-Shape Metrics}
\usage{
plot_curve_shape(
  shape_results,
  metrics = c("deficit_auc_normalized", "t50", "recovery_rate_k", "latency"),
  facet_by = "variable",
  drop_undetected = TRUE
)
}
\arguments{
\item{shape_results}{Output from \code{\link[=curve_shape_metrics]{curve_shape_metrics()}}.}

\item{metrics}{Metric columns to display.}

\item{facet_by}{Faceting columns.}

\item{drop_undetected}{Logical; drop rows where no stress was detected
(control series), which otherwise dominate the scale with noise.}
}
\value{
A \code{ggplot2} object.
}
\description{
Summarize the \code{\link[=curve_shape_metrics]{curve_shape_metrics()}} output as one panel per metric, so the
area, the speed and the latency of the response can be compared side by side
across genotypes and cycles.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = water_potential
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
plot_curve_shape(curve_shape_metrics(segments))
}
