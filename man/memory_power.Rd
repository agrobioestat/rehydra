% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power_functions.R
\name{memory_power}
\alias{memory_power}
\title{Replication Needed to Detect a Memory Effect}
\usage{
memory_power(
  effect,
  sd,
  n = NULL,
  power = NULL,
  alpha = 0.05,
  n_groups = 2,
  design = c("independent", "paired"),
  correlation = 0,
  adjust = c("none", "bonferroni", "tukey"),
  max_n = 1000
)
}
\arguments{
\item{effect}{Size of the difference to detect, in the units of the index
(for \code{Rs}, \code{0.05} means five percentage points of the pre-drought level).}

\item{sd}{Residual standard deviation of the index. Take it from
\code{rehydra_anova()$diagnostics$residual_se} of a pilot experiment, or from
\code{\link[=memory_power_pilot]{memory_power_pilot()}}.}

\item{n}{Replicates per cell. Give \code{n} to compute power, or leave \code{NULL} and
give \code{power} to solve for \code{n}.}

\item{power}{Target power. Give \code{power} to solve for \code{n}, or leave \code{NULL} and
give \code{n} to compute power.}

\item{alpha}{Significance level.}

\item{n_groups}{Number of cells being compared. Used only when
\code{adjust = "bonferroni"} or \code{"tukey"}.}

\item{design}{\code{"paired"} when the same plants are measured in both cycles
(the usual drought-memory design), \code{"independent"} when different plants
are used.}

\item{correlation}{Correlation between the two cycles measured on the same
plant, used when \code{design = "paired"}.}

\item{adjust}{Multiplicity correction: \code{"none"}, \code{"bonferroni"} or
\code{"tukey"}.}

\item{max_n}{Largest \code{n} searched when solving for replication.}
}
\value{
A tibble with \code{effect}, \code{sd}, \code{n}, \code{power}, \code{alpha}, \code{design},
\code{correlation}, \code{adjust}, \code{effect_size} and \code{df}.
}
\description{
How many replicates per genotype-by-treatment cell are needed to detect a
given change in an index between two drought cycles? Answering this before
the experiment is cheaper than discovering afterwards that a real effect went
undetected.
}
\details{
The comparison of an index between two cycles is a two-sample or paired
t-test on the cell means. Writing \eqn{\delta} for \code{effect} and \eqn{\sigma}
for \code{sd}, the standardized effect size is

\deqn{d = \frac{\delta}{\sigma\sqrt{c}}, \qquad
  c = \begin{cases} 2 & \text{independent} \\
  2(1 - \rho) & \text{paired}\end{cases}}

and the power of the test with \eqn{\nu} degrees of freedom is

\deqn{1 - \beta = P\left(|T_{\nu,\lambda}| > t_{1-\alpha/2,\,\nu}\right),
  \qquad \lambda = d\sqrt{n},}

computed from the non-central t distribution rather than from a normal
approximation, which matters at the replication levels ecophysiology
experiments actually use (4 to 8 pots per cell), where the normal
approximation overstates power appreciably.

The \code{design = "paired"} case is the one that usually applies and is worth
being explicit about: measuring cycle 1 and cycle 2 on the \emph{same} plant
removes the between-plant variance from the contrast, and the factor
\eqn{2(1-\rho)} shrinks the variance the test has to overcome. With a
correlation of \code{0.5} the required replication roughly halves relative to
independent groups. Estimate \eqn{\rho} from a pilot with
\code{\link[=memory_power_pilot]{memory_power_pilot()}}; assuming \code{0} when the design is paired is
conservative but wasteful.

\code{adjust} accounts for testing several cells at once: \code{"bonferroni"} divides
\code{alpha} by \code{n_groups}, and \code{"tukey"} uses the studentized range, which is
less conservative for all-pairs comparisons and matches what
\code{\link[=rehydra_anova]{rehydra_anova()}} reports.

Power analysis answers a design question, not a data question. Computing it
\emph{after} seeing the result, with the observed effect as \code{effect}, is
"observed power" and is a known fallacy: it is a deterministic function of
the p-value and adds nothing. Use a pilot, or the smallest effect that would
be biologically meaningful.
}
\examples{
# How many pots per cell to detect a 0.05 change in Rs, sd = 0.03?
memory_power(effect = 0.05, sd = 0.03, power = 0.8)

# Power actually available with 4 pots per cell
memory_power(effect = 0.05, sd = 0.03, n = 4)

# Same question for a paired design with a correlation of 0.5
memory_power(effect = 0.05, sd = 0.03, power = 0.8,
             design = "paired", correlation = 0.5)

# A range of scenarios at once
memory_power(effect = c(0.03, 0.05, 0.10), sd = 0.03, power = 0.8)
}
\seealso{
\code{\link[=rehydra_anova]{rehydra_anova()}}, \code{\link[=memory_effect]{memory_effect()}}, \code{\link[=memory_power_pilot]{memory_power_pilot()}}.
}
