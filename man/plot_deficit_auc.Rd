% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_curve_functions.R
\name{plot_deficit_auc}
\alias{plot_deficit_auc}
\title{Plot the Time Series with the Deficit Area Highlighted}
\usage{
plot_deficit_auc(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  facet_by = c("genotype", "variable"),
  baseline_summary = c("mean", "median", "last"),
  show_landmarks = TRUE,
  shape_results = NULL,
  fill = "#d98b8b"
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column.}

\item{group_by}{Grouping columns used to define one deficit curve.}

\item{facet_by}{Faceting columns.}

\item{baseline_summary}{How the baseline is summarized: \code{"mean"},
\code{"median"} or \code{"last"}.}

\item{show_landmarks}{Logical; mark the trough, the rewatering onset and
\code{t50}.}

\item{shape_results}{Optional pre-computed \code{\link[=curve_shape_metrics]{curve_shape_metrics()}} output.
Supplying it avoids recomputing the landmarks.}

\item{fill}{Fill colour of the shaded deficit area.}
}
\value{
A \code{ggplot2} object.
}
\description{
Draw the full trajectory together with the pre-drought baseline and shade the
area between them: the area under the deficit curve that
\code{\link[=curve_shape_metrics]{curve_shape_metrics()}} integrates. The trough, the rewatering onset and the
time to half recovery are marked so the numeric indices can be read straight
off the figure.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = stomatal_conductance
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)

drought_only <- segments[segments$treatment == "Drought" &
  segments$replicate == "1", ]
plot_deficit_auc(drought_only)
}
\seealso{
\code{\link[=curve_shape_metrics]{curve_shape_metrics()}}, \code{\link[=deficit_curve]{deficit_curve()}}.
}
