% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/references.R
\name{rehydra_check_dois}
\alias{rehydra_check_dois}
\title{Check Whether the Package DOIs Resolve}
\usage{
rehydra_check_dois(dois = NULL, timeout = 10)
}
\arguments{
\item{dois}{Character vector of DOIs. Defaults to the non-missing DOIs in
\code{\link[=rehydra_references]{rehydra_references()}}.}

\item{timeout}{Timeout in seconds for each request.}
}
\value{
A tibble with columns \code{doi}, \code{url}, \code{status} and \code{resolves}.
}
\description{
Interactive helper that sends one \code{HEAD}-like request per DOI to
\verb{https://doi.org} and reports which identifiers resolve.
}
\details{
This function requires an internet connection and is therefore \strong{never}
called from examples, tests or vignettes: CRAN machines must be able to run
the package fully offline. Use it manually before a submission, exactly as
\verb{R CMD check --as-cran} would.
}
\examples{
\dontrun{
rehydra_check_dois()
}
}
