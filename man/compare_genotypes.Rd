% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison_functions.R
\name{compare_genotypes}
\alias{compare_genotypes}
\title{Compare Genotypes for rehydra Metrics}
\usage{
compare_genotypes(
  data,
  metric = "Rs",
  genotype_col = "genotype",
  group_by = c("treatment", "variable", "cycle"),
  adjust_method = "BH",
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame containing genotype-level metrics.}

\item{metric}{Metric column to compare (string or bare column name).}

\item{genotype_col}{Genotype column name.}

\item{group_by}{Additional grouping columns used before pairwise comparison.}

\item{adjust_method}{Adjustment method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{conf_level}{Confidence level for pairwise confidence intervals.}
}
\value{
A list with two tibbles:
\itemize{
\item \code{pairwise}: pairwise differences, confidence intervals, p-values, and
adjusted p-values.
\item \code{ranking}: genotype ranking by mean metric in each group.
}
}
\description{
Perform pairwise genotype comparisons for resilience, damage, recovery,
stability, or memory metrics.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
res <- resilience_index(segments)
compare_genotypes(res, metric = "Rs")
}
