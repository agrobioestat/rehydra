% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/references.R
\name{rehydra_references}
\alias{rehydra_references}
\title{Methodological References Used by rehydra}
\usage{
rehydra_references()
}
\value{
A tibble with columns \code{key}, \code{authors}, \code{year}, \code{title}, \code{used_for},
\code{doi} and \code{doi_verified}.
}
\description{
Return the literature behind each family of indices implemented in the
package, together with the digital object identifier (DOI) of each work and
a flag telling whether that DOI has been verified as resolvable.
}
\details{
References with \code{doi_verified = FALSE} are deliberately shipped \strong{without}
a DOI string. \verb{R CMD check --as-cran} validates every DOI found in
\code{DESCRIPTION}, \verb{man/}, \code{inst/CITATION}, \code{README} and the vignettes, and
reports unresolvable identifiers as a \code{NOTE}, so an unverified DOI is left
empty instead of guessed. See the header of \code{R/references.R} for the
verification checklist.
}
\examples{
rehydra_references()

# Which references still need a verified DOI?
subset(rehydra_references(), !doi_verified)
}
\seealso{
\code{\link[=rehydra_check_dois]{rehydra_check_dois()}} to test resolution interactively.
}
