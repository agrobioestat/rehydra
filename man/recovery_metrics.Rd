% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recovery_functions.R
\name{recovery_metrics}
\alias{recovery_metrics}
\title{Calculate Recovery Metrics After Rewatering}
\usage{
recovery_metrics(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  recovery_threshold = 0.9,
  post_summary = c("mean", "median"),
  allow_overcompensation = TRUE
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for calculations.}

\item{group_by}{Grouping columns.}

\item{recovery_threshold}{Fraction of pre-drought performance used for
\code{time_to_recovery}.}

\item{post_summary}{Summary used to define post-drought level:
\code{"mean"} or \code{"median"}.}

\item{allow_overcompensation}{Logical; whether values above pre-drought
baseline are allowed and flagged.}
}
\value{
A tibble of recovery metrics by group and cycle.
}
\description{
Estimate the magnitude and speed of physiological recovery following drought.
}
\details{
Metrics include:
\itemize{
\item \code{post_rewatering_raw}
\item \code{post_rewatering_value}
\item \code{recovery_rate = (PostDrought - DroughtMinimum) / duration_recovery}
\item \code{max_recovery_rate} (maximum first derivative during recovery)
\item \code{recovery_completeness = PostDrought / PreDrought}
\item \code{residual_cost = 1 - (PostDrought / PreDrought)}
\item \code{recovery_auc}
\item \code{normalized_recovery_auc}
\item \code{time_to_recovery}
}
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
recovery_metrics(segments)
}
\references{
Xu et al. (2010) \url{doi:10.4161/psb.5.6.11398}.
}
