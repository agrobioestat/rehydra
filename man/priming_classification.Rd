% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_functions.R
\name{priming_classification}
\alias{priming_classification}
\title{Classify Priming Response Types}
\usage{
priming_classification(
  memory_results,
  threshold = 0.05,
  recovery_threshold = 0.9
)
}
\arguments{
\item{memory_results}{Output from \code{\link[=memory_effect]{memory_effect()}}.}

\item{threshold}{Minimum absolute change used to detect memory shifts.}

\item{recovery_threshold}{Threshold for incomplete recovery classification.}
}
\value{
Input tibble with \code{priming_class} appended.
}
\description{
Assign categorical priming classes based on memory and recovery metrics.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
res <- resilience_index(segments)
mem <- memory_effect(res)
priming_classification(mem)
}
