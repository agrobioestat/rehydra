% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segmentation_functions.R
\name{segment_drought_cycle}
\alias{segment_drought_cycle}
\title{Segment Drought-Rewatering Cycles}
\usage{
segment_drought_cycle(
  data,
  time,
  value,
  group_by = NULL,
  method = c("auto", "manual"),
  events = NULL,
  smoothing = TRUE,
  smooth_span = 0.25,
  min_drop = 0.15,
  min_recovery = 0.1,
  n_cycles = NULL,
  response_direction = c("higher_is_better", "lower_is_better", "more_negative_is_worse",
    "auto"),
  fallback_manual = TRUE
)
}
\arguments{
\item{data}{A standardized data frame, typically from
\code{\link[=prepare_rehydra_data]{prepare_rehydra_data()}}.}

\item{time}{Time column.}

\item{value}{Value column used for segmentation.}

\item{group_by}{Grouping columns.}

\item{method}{Segmentation method: \code{"auto"} or \code{"manual"}.}

\item{events}{Optional event table for manual segmentation.}

\item{smoothing}{Logical; apply smoothing before automatic segmentation.}

\item{smooth_span}{Loess span used when \code{smoothing = TRUE}.}

\item{min_drop}{Minimum relative drop from baseline to mark drought.}

\item{min_recovery}{Minimum relative recovery after rewatering.}

\item{n_cycles}{Optional expected number of cycles.}

\item{response_direction}{Physiological response direction mode.}

\item{fallback_manual}{Logical; fallback to manual heuristic when
automatic segmentation is uncertain.}
}
\value{
A tibble with original data and added columns:
\code{cycle}, \code{phase}, \code{phase_id}, \code{event}, \code{confidence}, and \code{method}.
}
\description{
Segment plant time series into drought and rewatering phases using manual
events or automatic heuristics.
}
\details{
Required phase labels are \code{PreDrought}, \code{Drought}, \code{Rewatering}, and
\code{PostDrought}. \code{RecoveryPlateau} can be assigned manually.

Automatic mode uses smoothed trajectories, first derivatives, local minima,
and relative baseline drop/recovery thresholds. If detection confidence is
low, the function emits an informative warning and optionally falls back to a
deterministic manual heuristic. If \code{fallback_manual = FALSE}, automatic
detection failures return an informative error.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)

segments <- segment_drought_cycle(
  data = prepared,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "variable"),
  method = "auto"
)

head(segments)
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x};
Xu et al. (2010) \url{doi:10.4161/psb.5.6.11398};
Ribeiro et al. (2021) \url{doi:10.1016/j.jplph.2021.153397}.
}
