% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_shape_functions.R
\name{deficit_curve}
\alias{deficit_curve}
\title{Deficit Curve of One Cycle}
\usage{
deficit_curve(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  baseline_summary = c("mean", "median", "last")
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for the calculations.}

\item{group_by}{Grouping columns.}

\item{baseline_summary}{How the pre-drought baseline is summarized:
\code{"mean"}, \code{"median"} or \code{"last"}.}
}
\value{
A tibble with the grouping columns plus \code{time}, \code{value}, \code{baseline},
\code{deficit}, \code{cumulative_deficit} and \code{phase}.
}
\description{
Return the point-by-point deficit \code{d(t) = max(0, B - y(t))} used by
\code{\link[=curve_shape_metrics]{curve_shape_metrics()}}, together with the running (cumulative) area. This is
what \code{\link[=plot_deficit_auc]{plot_deficit_auc()}} shades, and it is exported so the shaded region can
be reproduced or exported for a custom figure.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
head(deficit_curve(segments))
}
