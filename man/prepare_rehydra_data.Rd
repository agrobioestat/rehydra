% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_functions.R
\name{prepare_rehydra_data}
\alias{prepare_rehydra_data}
\title{Prepare Data for rehydra Analysis}
\usage{
prepare_rehydra_data(
  data,
  time,
  genotype,
  treatment,
  replicate,
  variables = NULL,
  plant_id = NULL,
  block = NULL,
  plot_id = NULL,
  pot_id = NULL,
  cycle = NULL,
  phase = NULL,
  variable = NULL,
  value = NULL,
  response_direction = c("higher_is_better", "lower_is_better", "more_negative_is_worse",
    "auto"),
  transform = c("none", "absolute", "invert", "stress_score", "relative_to_control"),
  control_label = "Control"
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{time}{Time column (numeric, \code{Date}, or \code{POSIXct}).}

\item{genotype}{Genotype column.}

\item{treatment}{Treatment column, usually including \code{"Control"} and
\code{"Drought"}.}

\item{replicate}{Replicate column.}

\item{variables}{Wide-format variable columns to pivot into
\code{variable} and \code{value}.}

\item{plant_id}{Optional plant identifier column.}

\item{block}{Optional block column.}

\item{plot_id}{Optional plot identifier column.}

\item{pot_id}{Optional pot identifier column.}

\item{cycle}{Optional cycle column.}

\item{phase}{Optional phase column.}

\item{variable}{Optional variable-name column if \code{data} is already long.}

\item{value}{Optional value column if \code{data} is already long.}

\item{response_direction}{One of \code{"higher_is_better"},
\code{"lower_is_better"}, \code{"more_negative_is_worse"}, or \code{"auto"}.}

\item{transform}{One of \code{"none"}, \code{"absolute"}, \code{"invert"},
\code{"stress_score"}, or \code{"relative_to_control"}.}

\item{control_label}{Label used for the control treatment when
\code{transform = "relative_to_control"}.}
}
\value{
A standardized tibble with columns:
\code{plant_id}, \code{genotype}, \code{treatment}, \code{replicate}, \code{block}, \code{plot_id},
\code{pot_id}, \code{time}, \code{cycle}, \code{phase}, \code{variable}, \code{value},
\code{response_direction}, and \code{transformed_value}.
}
\description{
Standardize plant drought-rewatering data into the internal long format used
by \code{rehydra}.
}
\details{
Input can be wide or long:
\itemize{
\item Wide: pass columns in \code{variables = c(var1, var2, ...)}.
\item Long: pass \code{variable = ...} and \code{value = ...}.
}

\code{response_direction} and \code{transform} are applied before downstream
calculations. Rt, Rc, and Rs are directly interpretable when
\code{transformed_value} increases with better physiological performance.
For variables such as water potential, use direction-aware transformation
when needed. Internally, transformed values are shifted to a positive
performance scale to stabilize ratio-based indices.
}
\examples{
data(rehydra_data)

prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)

head(prepared)
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x};
Xu et al. (2010) \url{doi:10.4161/psb.5.6.11398};
Ribeiro et al. (2021) \url{doi:10.1016/j.jplph.2021.153397}.
}
