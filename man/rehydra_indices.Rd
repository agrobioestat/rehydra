% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/indices_functions.R
\name{rehydra_indices}
\alias{rehydra_indices}
\title{Every Index Family in One Table}
\usage{
rehydra_indices(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  families = c("resilience", "damage", "recovery", "recovery_period", "mean_reduction",
    "shape", "stability"),
  prefix = FALSE,
  ...
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for the calculations.}

\item{group_by}{Grouping columns; the join key is these columns.}

\item{families}{Which families to include. Any of \code{"resilience"},
\code{"damage"}, \code{"recovery"}, \code{"recovery_period"}, \code{"mean_reduction"},
\code{"shape"}, \code{"stability"}.}

\item{prefix}{Logical; prefix each column with its family (\code{Rs} becomes
\code{resilience_Rs}). Useful when several families expose columns with the same
name.}

\item{...}{Passed to every underlying metric function, so shared arguments
such as \code{baseline_summary} stay consistent across families.}
}
\value{
A tibble with one row per group and cycle.
}
\description{
Compute the resilience, damage, recovery, recovery-period, mean-reduction and
curve-shape families in a single call and return them joined on the group-and-cycle key,
one row per group per cycle.
}
\details{
The families overlap on purpose: \code{baseline}, \code{drought_minimum} and
\code{stress_detected} are computed from the same landmarks by
\code{\link[=recovery_period_metrics]{recovery_period_metrics()}}, \code{\link[=mean_reduction_metrics]{mean_reduction_metrics()}} and \code{\link[=curve_shape_metrics]{curve_shape_metrics()}}, and
\code{PreDrought} appears in both \code{\link[=damage_metrics]{damage_metrics()}} and \code{\link[=recovery_metrics]{recovery_metrics()}}.
Duplicated columns are kept only once, from the first family that provides
them, unless \code{prefix = TRUE}. Because all families read one shared geometry
the duplicates are identical by construction, which the package tests check.

The result is the natural input to \code{\link[=rehydra_anova]{rehydra_anova()}} and \code{\link[=memory_trend]{memory_trend()}}:
both take a per-replicate index table and a \code{response}/\code{metrics} argument, so
one call here gives every index they can be pointed at.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = water_potential
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)

idx <- rehydra_indices(segments)
dim(idx)
idx[, c("genotype", "cycle", "Rs", "deficit_auc", "mean_reduction")]
}
\seealso{
\code{\link[=summarize_rehydra]{summarize_rehydra()}} for the full pipeline including memory and
segmentation.
}
