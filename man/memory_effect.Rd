% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_functions.R
\name{memory_effect}
\alias{memory_effect}
\title{Calculate Drought Memory (Priming) Effects}
\usage{
memory_effect(
  resilience_results,
  damage_results = NULL,
  recovery_results = NULL,
  cycle_ref = 1,
  cycle_test = 2,
  group_by = c("genotype", "treatment", "variable"),
  ci_method = c("none", "bootstrap"),
  n_boot = 999,
  conf_level = 0.95
)
}
\arguments{
\item{resilience_results}{Output from \code{\link[=resilience_index]{resilience_index()}}.}

\item{damage_results}{Optional output from \code{\link[=damage_metrics]{damage_metrics()}}.}

\item{recovery_results}{Optional output from \code{\link[=recovery_metrics]{recovery_metrics()}}.}

\item{cycle_ref}{Reference cycle.}

\item{cycle_test}{Test cycle, or \code{"previous"}, or \code{"mean_later"}.}

\item{group_by}{Grouping columns.}

\item{ci_method}{Confidence interval method: \code{"none"} or \code{"bootstrap"}.}

\item{n_boot}{Number of bootstrap iterations.}

\item{conf_level}{Confidence level.}
}
\value{
A tibble with cycle comparison metrics, optional index-specific
interval columns (\verb{conf_low_*}, \verb{conf_high_*}), and interpretation labels.
}
\description{
Compare resilience and optional damage/recovery metrics across drought cycles
to quantify physiological memory.
}
\details{
Core memory metrics:
\itemize{
\item \code{Mem_Rt = Rt_test - Rt_ref}
\item \code{Mem_Rc = Rc_test - Rc_ref}
\item \code{Mem_Rs = Rs_test - Rs_ref}
\item \code{Mem_Damage = damage_auc_ref - damage_auc_test}
\item \code{Mem_RecRate = recovery_rate_test - recovery_rate_ref}
\item \code{Mem_Residual = residual_cost_ref - residual_cost_test}
\item Relative ratios (\code{Mem_Ratio_Rt}, \code{Mem_Ratio_Rc}, \code{Mem_Ratio_Rs})
}

Positive values for \code{Mem_Rt} and \code{Mem_Rc} indicate improved resistance or
recovery in later cycles.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
res <- resilience_index(segments)
memory_effect(res)
}
\references{
Ribeiro et al. (2021) \url{doi:10.1016/j.jplph.2021.153397}.
}
