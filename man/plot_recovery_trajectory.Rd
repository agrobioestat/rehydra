% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_recovery_trajectory}
\alias{plot_recovery_trajectory}
\title{Plot Drought-Rewatering Trajectories}
\usage{
plot_recovery_trajectory(
  data,
  time = time,
  value = transformed_value,
  id_col = "plant_id",
  phase_colors = NULL,
  show_points = TRUE,
  show_mean = TRUE,
  show_ci = TRUE,
  facet_by = c("genotype", "variable"),
  annotate_indices = TRUE,
  resilience_results = NULL
)
}
\arguments{
\item{data}{Segmented data.}

\item{time}{Time column.}

\item{value}{Value column.}

\item{id_col}{Column used for individual lines.}

\item{phase_colors}{Named vector of colors for phases.}

\item{show_points}{Logical; show points.}

\item{show_mean}{Logical; overlay group means.}

\item{show_ci}{Logical; include standard error ribbon for group means.}

\item{facet_by}{Faceting columns.}

\item{annotate_indices}{Logical; annotate Rt and Rc when
\code{resilience_results} is provided.}

\item{resilience_results}{Optional output from \code{\link[=resilience_index]{resilience_index()}}.}
}
\value{
A \code{ggplot2} object.
}
\description{
Plot ecophysiological trajectories with phase shading and optional resilience
annotations.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
plot_recovery_trajectory(segments)
}
