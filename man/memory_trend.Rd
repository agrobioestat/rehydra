% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_trend.R
\name{memory_trend}
\alias{memory_trend}
\title{Between-Cycle Memory Trend Across Three or More Cycles}
\usage{
memory_trend(
  indices,
  metrics = NULL,
  group_by = c("genotype", "treatment", "variable"),
  min_cycles = 3,
  conf_level = 0.95,
  p_adjust = "holm",
  alpha = 0.05
)
}
\arguments{
\item{indices}{A per-cycle index table with a \code{cycle} column, typically the
output of \code{\link[=resilience_index]{resilience_index()}}, \code{\link[=curve_shape_metrics]{curve_shape_metrics()}},
\code{\link[=recovery_period_metrics]{recovery_period_metrics()}} or \code{\link[=mean_reduction_metrics]{mean_reduction_metrics()}}.}

\item{metrics}{Character vector of numeric columns to analyse. Defaults to
whichever of \code{Rt}, \code{Rc}, \code{Rs} are present.}

\item{group_by}{Grouping columns defining an independent memory trajectory.}

\item{min_cycles}{Minimum number of distinct cycles required to fit a trend.}

\item{conf_level}{Confidence level for the slope interval.}

\item{p_adjust}{Method passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} for the slope p-values,
applied across metrics within each group.}

\item{alpha}{Significance level used by the \code{memory_class} label.}
}
\value{
A tibble with one row per group and metric, with columns for the
grouping variables plus \code{metric}, \code{n_cycles}, \code{n_obs}, \code{first_cycle},
\code{last_cycle}, \code{first_value}, \code{last_value}, \code{cumulative_change}, \code{slope},
\code{slope_se}, \code{slope_conf_low}, \code{slope_conf_high}, \code{slope_relative},
\code{statistic}, \code{p_value}, \code{p_value_adj}, \code{r_squared}, \code{monotonic_fraction},
\code{lower_is_better} and \code{memory_class}.
}
\description{
Quantify how an index changes as drought-rewatering cycles accumulate.
\code{\link[=memory_effect]{memory_effect()}} compares two cycles at a time; \code{memory_trend()} fits the
whole sequence and answers whether memory is being \emph{built up}, \emph{eroded} or is
\emph{stable} over repeated exposure.
}
\details{
For each group and metric the index values of the individual replicates are
regressed on the cycle number,

\deqn{y_{ij} = \beta_0 + \beta_1 c_i + \varepsilon_{ij},}

where \eqn{c_i} is the cycle index and \eqn{j} runs over replicates. The
slope \eqn{\beta_1} is the \strong{memory rate}: the average change in the index
per additional drought cycle. Fitting on the replicate-level rows rather than
on the per-cycle means keeps the residual degrees of freedom that the
experiment actually has, so the standard error and the p-value are honest;
regressing three cycle means on three cycle numbers would leave one degree of
freedom and produce a p-value that means nothing.

Three complementary numbers are reported because a slope alone can be
misleading:
\itemize{
\item \code{slope} with its interval and p-value: the average per-cycle change.
\item \code{cumulative_change} = value in the last cycle minus value in the first:
the total memory accumulated, which is what a physiologist reports.
\item \code{monotonic_fraction}: the proportion of consecutive cycle-to-cycle steps
that move in the same direction as the slope. A significant slope with a
\code{monotonic_fraction} near \code{0.5} is being driven by the endpoints rather
than by a consistent progression, which usually means one anomalous cycle,
not memory.
}

\code{slope_relative} divides the slope by the mean level of the index, giving a
per-cycle proportional change that is comparable between metrics measured on
different scales (a slope of \code{0.02} means one thing for \code{Rs} and another for
\code{deficit_auc}).

The \code{memory_class} label combines direction and significance:
\code{"acquired_memory"} (index improving significantly across cycles),
\code{"eroded_memory"} (index deteriorating significantly), \code{"stable"} (no
significant change) and \code{"undetermined"} (too few cycles or a degenerate
fit). For metrics where a \emph{lower} value is better — \code{deficit_auc},
\code{total_reduction}, \code{mean_reduction}, \code{t50}, \code{residual_deficit} and the like —
the direction is flipped automatically, so \code{"acquired_memory"} always means
"the plant handled the later cycles better". Pass \code{metrics} explicitly with
\code{\link[=memory_direction]{memory_direction()}} if you need to override that mapping.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
res <- resilience_index(segments)

# The bundled example has two cycles, so min_cycles must be lowered.
memory_trend(res, min_cycles = 2)
}
\references{
Ribeiro et al. (2021) \url{doi:10.1016/j.jplph.2021.153397}; Lloret et al. (2011)
\url{doi:10.1111/j.1600-0706.2011.19372.x}.
}
\seealso{
\code{\link[=memory_effect]{memory_effect()}} for pairwise cycle contrasts,
\code{\link[=plot_memory_trend]{plot_memory_trend()}} for the figure.
}
