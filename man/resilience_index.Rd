% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resilience_functions.R
\name{resilience_index}
\alias{resilience_index}
\title{Calculate Resistance, Recovery, and Resilience Indices}
\usage{
resilience_index(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  drought_summary = c("minimum", "mean", "last"),
  predrought_summary = c("mean", "median", "last"),
  postdrought_summary = c("mean", "median", "last"),
  conf_level = 0.95,
  ci_method = c("none", "bootstrap"),
  n_boot = 999
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for calculations.}

\item{group_by}{Grouping columns.}

\item{drought_summary}{Summary method for drought level:
\code{"minimum"}, \code{"mean"}, or \code{"last"}.}

\item{predrought_summary}{Summary method for pre-drought level:
\code{"mean"}, \code{"median"}, or \code{"last"}.}

\item{postdrought_summary}{Summary method for post-drought level:
\code{"mean"}, \code{"median"}, or \code{"last"}.}

\item{conf_level}{Confidence level for bootstrap intervals.}

\item{ci_method}{Either \code{"none"} or \code{"bootstrap"}.}

\item{n_boot}{Number of bootstrap resamples.}
}
\value{
A tibble with \code{PreDrought}, \code{Drought}, \code{PostDrought}, \code{Rt}, \code{Rc},
\code{Rs}, \code{standard_error}, \code{conf_low}, \code{conf_high}, index-specific bootstrap
interval columns (\verb{*_Rt}, \verb{*_Rc}, \verb{*_Rs}), and \code{method}.
}
\description{
Compute Rt, Rc, and Rs following Lloret et al. (2011).
}
\details{
Formulas:
\itemize{
\item \code{Rt = Drought / PreDrought}
\item \code{Rc = PostDrought / Drought}
\item \code{Rs = PostDrought / PreDrought}
}

These ratios are directly interpretable when higher values indicate better
physiological performance. For variables such as water potential,
apply a direction-aware transformation before interpretation.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
resilience_index(segments)
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x}.
}
