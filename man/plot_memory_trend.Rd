% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_curve_functions.R
\name{plot_memory_trend}
\alias{plot_memory_trend}
\title{Plot the Between-Cycle Memory Trend}
\usage{
plot_memory_trend(
  indices,
  trend_results = NULL,
  metrics = NULL,
  group_by = c("genotype", "treatment", "variable"),
  facet_by = c("variable")
)
}
\arguments{
\item{indices}{A per-cycle index table, as passed to \code{\link[=memory_trend]{memory_trend()}}.}

\item{trend_results}{Optional pre-computed \code{\link[=memory_trend]{memory_trend()}} output, used to
colour the panels by memory class.}

\item{metrics}{Metric columns to plot.}

\item{group_by}{Grouping columns.}

\item{facet_by}{Faceting columns.}
}
\value{
A \code{ggplot2} object.
}
\description{
Show each index against the cycle number with the fitted memory slope, the
quantity \code{\link[=memory_trend]{memory_trend()}} tests.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = water_potential
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
res <- resilience_index(segments)
plot_memory_trend(res, memory_trend(res, min_cycles = 2))
}
