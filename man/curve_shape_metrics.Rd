% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curve_shape_functions.R
\name{curve_shape_metrics}
\alias{curve_shape_metrics}
\title{Damage and Recovery Curve-Shape Metrics}
\usage{
curve_shape_metrics(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  latency_fraction = 0.05,
  damage_fraction = 0.05,
  baseline_summary = c("mean", "median", "last"),
  fit_rate = TRUE
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for the calculations.}

\item{group_by}{Grouping columns.}

\item{latency_fraction}{Fraction of the deficit amplitude that the response
must regain before recovery counts as started. Default \code{0.05}.}

\item{damage_fraction}{Fraction of the deficit amplitude that must be lost
before damage counts as started. Default \code{0.05}.}

\item{baseline_summary}{How the pre-drought baseline is summarized:
\code{"mean"}, \code{"median"} or \code{"last"}.}

\item{fit_rate}{Logical; fit the exponential recovery rate constant. Set to
\code{FALSE} to skip the nonlinear fit on very large datasets.}
}
\value{
A tibble with one row per group and cycle containing the landmark
columns (\code{baseline}, \code{drought_minimum}, \code{stress_detected},
\code{deficit_amplitude},
\code{time_minimum}, \code{time_rewatering}), the areas (\code{deficit_auc},
\code{deficit_auc_damage}, \code{deficit_auc_recovery}, \code{deficit_auc_normalized},
\code{auc_damage_share}), the timings (\code{t50}, \code{t50_damage},
\code{recovery_symmetry}, \code{latency}, \code{lag_to_minimum}, \code{damage_latency}), the
exponential fit (\code{recovery_rate_k}, \code{recovery_half_life},
\code{rate_r_squared}, \code{rate_method}, \code{rate_n_points}) and the endpoint
descriptors (\code{residual_deficit}, \code{overshoot}).
}
\description{
Describe the \emph{shape} of a drought-rewatering trajectory rather than only its
endpoints: how much performance was lost (area under the deficit curve), how
fast half of it came back (\code{t50}), how steeply the remaining deficit decays
(the exponential rate constant \code{k}) and how long the plant kept falling after
water was restored (latency).
}
\section{The mathematics behind the landmarks}{
Every metric below is read off the same five landmarks, extracted from the
\emph{whole} time series of one cycle rather than from a handful of
phase averages. Let \code{B} be the baseline (pre-drought level), \code{y(t)} the
trajectory, \code{t_str} the onset of the stress, \code{t_rew} the onset of rewatering,
and

\deqn{(t_{min},\, v_{min}) = \arg\min_{t \in \mathrm{Drought} \cup
  \mathrm{Rewatering}} y(t)}

the trough. Searching the trough across \emph{both} phases is deliberate: leaf
water potential and gas exchange routinely keep falling for some hours after
irrigation resumes, so restricting the search to the drought phase would
place the minimum too early and inflate every recovery metric. Write
\eqn{A = B - v_{min}} for the deficit amplitude.
\subsection{Area under the deficit curve (AUC)}{

The deficit function is the non-negative distance to the baseline,

\deqn{d(t) = \max(0,\; B - y(t)),}

and its integral is evaluated with the trapezoidal rule on the observed grid
\eqn{t_1 < \dots < t_n},

\deqn{\int d(t)\,dt \approx \sum_{i=1}^{n-1}
  \frac{d(t_i) + d(t_{i+1})}{2}\,(t_{i+1} - t_i),}

which is exact for a piecewise-linear trajectory and, unlike a simple mean of
the observations, is correct under irregular sampling. Three areas are
reported: \code{deficit_auc} over the whole cycle, \code{deficit_auc_damage} over
\eqn{[t_{str}, t_{min}]} and \code{deficit_auc_recovery} over
\eqn{[t_{min}, t_{end}]}. Their ratio \code{auc_damage_share} says whether the cost
of the episode was paid while drying down or while recovering — two plants
with the same total AUC but opposite shares behave very differently.
\code{deficit_auc_normalized} divides the total by \eqn{B \times (t_{end} -
t_{str})}, making it a dimensionless fraction of the undisturbed integral.

Clipping at zero is what makes the AUC a damage measure: without it,
overcompensation after rewatering would cancel out earlier losses.
}

\subsection{Time to half recovery (t50)}{

\deqn{t_{50} = \min\{t \ge t_{min} : y(t) \ge v_{min} + A/2\} - t_{min},}

the time needed to close half of the gap. The crossing is found by linear
interpolation between the two bracketing observations,

\deqn{t^{*} = t_i + (L - y_i)\frac{t_{i+1} - t_i}{y_{i+1} - y_i},}

so \code{t50} has finer resolution than the sampling interval. Because it is
anchored on the amplitude and not on \code{B}, \code{t50} is a pure speed descriptor:
a plant that recovers halfway quickly and then stalls has a small \code{t50} and a
large \code{residual_deficit}, and the pair identifies that behaviour, which a
single endpoint ratio such as \code{Rs} cannot.

The mirror-image \code{t50_damage} is the time from \code{t_str} to lose half of the
amplitude, and \code{recovery_symmetry} = \eqn{t_{50} / (t_{50} + t_{50,damage})}
places the cycle on a scale where \code{0.5} means damage and recovery took
equally long, above \code{0.5} means recovery lagged behind the collapse.
}

\subsection{Recovery rate constant (k)}{

The remaining deficit is assumed to decay exponentially once rewatering takes
effect,

\deqn{y(t) = B - A\,e^{-k\,(t - t_{min})},}

so that the deficit ratio is scale free,

\deqn{\frac{B - y(t)}{A} = e^{-k (t - t_{min})}.}

Taking logarithms linearizes it into a no-intercept regression of
\eqn{z = \log((B - y)/A)} on \eqn{u = t - t_{min}} whose closed-form slope

\deqn{k_0 = -\frac{\sum u\,z}{\sum u^2}}

is used as the starting value for a nonlinear least-squares refit on the
untransformed scale. The refit matters because the log transform gives the
near-baseline points — where the measurement noise is proportionally largest
— as much weight as the large early deficits that actually carry the signal.
\code{rate_method} records which estimate was kept, and \code{rate_r_squared} the fit
quality on the original scale; a low value is a warning that the return was
not exponential (a plateau, a rebound, or a second stress).
\code{recovery_half_life} \eqn{= \log 2 / k} restates \code{k} in time units.

\code{k} and \code{t50} answer different questions: \code{t50} is model free and always
defined, \code{k} assumes a shape but summarizes the entire branch in one number
and is what makes recovery curves comparable between experiments with
different sampling designs.
}

\subsection{Temporal latency}{

\deqn{\mathrm{latency} = \min\{t \ge a : y(t) \ge v_{min} +
  \varepsilon A\} - t_{rew}, \qquad a = \max(t_{min},\, t_{rew}),}

with \eqn{\varepsilon =} \code{latency_fraction}: the delay between restoring water
and the first measurable upturn. The anchor \eqn{a} is whichever of the
trough and the rewatering onset comes last, and that detail is what keeps the
metric interpretable in both situations that occur in practice. When the
response keeps falling after irrigation the trough lies inside the rewatering
window and the latency measures exactly that continued decline; when the
trough is instead the last drought observation, the search starts at the
rewatering onset rather than before it, which would otherwise return a
negative delay. Either way the metric isolates the inertia of the system from
the speed of the recovery that follows.

The companion \code{lag_to_minimum} \eqn{= t_{min} - t_{rew}} reports how long the
decline continued past rewatering, and is negative when the trough was
reached before the phase label changed. \code{damage_latency} is the symmetric
delay between the onset of the stress and the first measurable decline, a
proxy for how much buffering (stored soil water, hydraulic capacitance) the
plant had.
}
}

\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)

shape <- curve_shape_metrics(segments)
head(shape)

# Speed of recovery is not the same as completeness of recovery:
shape[, c("t50", "recovery_rate_k", "residual_deficit")]
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x};
Xu et al. (2010) \url{doi:10.4161/psb.5.6.11398}.
}
\seealso{
\code{\link[=recovery_period_metrics]{recovery_period_metrics()}}, \code{\link[=mean_reduction_metrics]{mean_reduction_metrics()}}, \code{\link[=plot_deficit_auc]{plot_deficit_auc()}}.
}
