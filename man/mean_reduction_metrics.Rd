% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/period_reduction_functions.R
\name{mean_reduction_metrics}
\alias{mean_reduction_metrics}
\title{Mean-Recovery-Rate and Mean-Reduction Indices}
\usage{
mean_reduction_metrics(
  data,
  time = time,
  value = transformed_value,
  group_by = c("genotype", "treatment", "replicate", "plant_id", "variable", "cycle"),
  recovery_level = 0.95,
  reduction_level = 0.95,
  baseline_summary = c("mean", "median", "last")
)
}
\arguments{
\item{data}{Segmented data from \code{\link[=segment_drought_cycle]{segment_drought_cycle()}}.}

\item{time}{Time column.}

\item{value}{Value column used for the calculations.}

\item{group_by}{Grouping columns.}

\item{recovery_level}{Fraction of the baseline that closes the recovery
period.}

\item{reduction_level}{Fraction of the baseline below which the response
counts as impaired.}

\item{baseline_summary}{How the pre-drought baseline is summarized:
\code{"mean"}, \code{"median"} or \code{"last"}.}
}
\value{
A tibble with one row per group and cycle and columns \code{baseline},
\code{drought_minimum}, \code{stress_detected}, \code{reduction_period}, \code{mean_reduction},
\code{mean_reduction_absolute}, \code{max_reduction}, \code{recovery_period},
\code{recovery_censored}, \code{total_recovery}, \code{mean_recovery_rate} and
\code{mean_recovery_rate_relative}.
}
\description{
Compute the average rate at which a plant returns to its pre-drought level
and the average intensity of the impairment while it lasted.
}
\details{
With the same notation as \code{\link[=recovery_period_metrics]{recovery_period_metrics()}} (\code{B} baseline, \code{y(t)} trajectory,
\code{t_min} the trough, \code{v_min = y(t_min)}):

\strong{Mean recovery rate.} Total amplitude regained divided by the time it took
to regain it,

\deqn{MRR = \frac{y(t_{rec}) - v_{min}}{t_{rec} - t_{min}},}

the average slope of the chord that joins the trough to the moment of
recovery. Unlike \code{max_recovery_rate} in \code{\link[=recovery_metrics]{recovery_metrics()}}, which is the
steepest observed first difference and is therefore sensitive to a single
noisy pair of points, \code{MRR} integrates over the whole return and is stable.
\code{mean_recovery_rate_relative} divides it by \code{B}, giving a
fraction-of-baseline per time unit that is comparable across variables.

When the series ends before the recovery threshold is reached, the rate is
computed over the observed part of the return,
\eqn{(y(t_{end}) - v_{min}) / (t_{end} - t_{min})}, and \code{recovery_censored}
is \code{TRUE}. The value is a valid lower bound on the true rate only if recovery
is monotone, so censored rows should be reported as such rather than pooled
silently with complete ones.

\strong{Mean reduction.} The average relative deficit over the time the plant
actually spent impaired,

\deqn{MR = \frac{1}{T_{red}} \int_{\{t\,:\,y(t) \le \beta B\}}
  \frac{B - y(t)}{B}\, dt,}

with \eqn{\beta =} \code{reduction_level} and \eqn{T_{red}} the length of that
impaired window (\code{reduction_period}). Dividing by \eqn{T_{red}} rather than by
the full cycle is what separates \emph{intensity} from \emph{duration}: a short severe
episode and a long mild one can share the same \code{total_reduction} from
\code{\link[=recovery_period_metrics]{recovery_period_metrics()}} but are told apart by \code{mean_reduction} (high, short
\code{reduction_period}) versus (low, long \code{reduction_period}). Reporting the pair
is more informative than either alone.

\code{max_reduction} is the depth of the trough, \eqn{(B - v_{min})/B}, the
complement of the resistance index \code{Rt} of \code{\link[=resilience_index]{resilience_index()}}.
}
\examples{
data(rehydra_data)
prepared <- prepare_rehydra_data(
  data = rehydra_data,
  time = time_days,
  genotype = genotype,
  treatment = treatment,
  replicate = replicate,
  variables = c(water_potential, stomatal_conductance)
)
segments <- segment_drought_cycle(prepared, time = time, value = transformed_value)
mean_reduction_metrics(segments)
}
\references{
Lloret et al. (2011) \url{doi:10.1111/j.1600-0706.2011.19372.x}.
}
\seealso{
\code{\link[=recovery_period_metrics]{recovery_period_metrics()}}, \code{\link[=curve_shape_metrics]{curve_shape_metrics()}}.
}
